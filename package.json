{
  "name": "v20-automation",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "cross-env TEST_ENV=sandbox npx playwright test --grep @ui",
    "test:record": "npx playwright codegen",
    "test:report": "npx playwright show-report"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@playwright/test": "^1.24.2",
    "allure-playwright": "^2.0.0-beta.18",
    "winston": "^3.8.1"
  },
  "dependencies": {
    "dotenv": "^16.0.1"
  }
}
